{
  "F24": {"verdict":"refuted","refute_reason":"already-fixed","classification":"concurrency","evidence":"knowledge-id-allocator.ts:50-65 (ISS-013): allocate() read→mutate→atomic-write wrapped in withFileLock(${metaPath}.lock); cross+in-process serialized. Test concurrent_allocation proves N parallel allocate() mint distinct ids."},
  "F25": {"verdict":"refuted","refute_reason":"already-fixed","classification":"data-loss","evidence":"knowledge-id-allocator.ts:90-115 (ISS-014): corrupt JSON now quarantines to .corrupted.{ts} sidecar and THROWS GenericIOError instead of returning empty meta — original file preserved, no silent overwrite. Test corrupt_meta_preservation covers it."},
  "F39": {"verdict":"confirmed","classification":"concurrency","fix_decision":"FIX-NOW","fix_task_id":"T9","evidence":"writeKnowledgeMeta (knowledge-meta-builder.ts:139) did read-rebuild-write of agents.meta.json preserving counters from a possibly-stale snapshot WITHOUT the allocator's ${metaPath}.lock → could clobber a concurrent allocate()'s counter bump → re-mint duplicate stable_id. Reachable under user's multi-window concurrent-dev. FIXED: wrapped in withFileLock."},
  "F30": {"verdict":"confirmed","classification":"concurrency","fix_decision":"FIX-NOW","fix_task_id":"T9","evidence":"doctor.ts fixCounterDesync(:8135) + applyIndexDriftFix(:2334) did unlocked read→atomicWriteJson of counters → same clobber class as F39 (sibling write sites). FIXED: both wrapped in withFileLock(${metaPath}.lock)."},
  "F31": {"verdict":"confirmed","classification":"concurrency","fix_decision":"FIX-NOW","fix_task_id":"T9","evidence":"doctor --fix reconcileKnowledge(writeKnowledgeMeta) + fixCounterDesync were two independent unlocked atomic writes. Each meta write now holds ${metaPath}.lock; combined with the deliberate before/after fixCounterDesync ordering (doctor.ts:1540-1618) the counter-clobber window is closed. Cross-step intermediate visibility is self-healing via the post-reconcile fixCounterDesync (acceptable; full doctor-run lock would block live MCP allocate for seconds)."}
}

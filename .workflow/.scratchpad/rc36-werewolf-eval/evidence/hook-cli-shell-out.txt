12:  // observability; the rest of fabric-hint must still function.
43:// contract or skip:<reason>. Reads .fabric/agents.meta.json (via
60:// node_modules access, so it cannot import from @fenglimg/fabric-server.
61:const FABRIC_DIR = ".fabric";
74:// `.fabric/.cache/edit-counter` (one ISO-8601 line per PreToolUse fire,
77:// archive_edit_threshold (default 20, configurable via fabric-config.json).
85:// Safe-degrade contract: if `.fabric/.cache/edit-counter` is missing or every
91:// review_hint_pending_age_days are now read from .fabric/fabric-config.json.
98:const EDIT_COUNTER_FILE_REL = join(".fabric", ".cache", "edit-counter");
109:// consume these to decide when a "run fabric doctor" reminder fires; T7 only
116:// rc.5 TASK-010: third signal — underseeded knowledge corpus → fabric-import skill.
129:// Override via .fabric/fabric-config.json#archive_hint_cooldown_hours.
130:const CONFIG_FILE = "fabric-config.json";
136:const SHOWN_CACHE_FILE = ".fabric/.cache/archive-hint-shown.json";
142:const MAINTENANCE_HINT_LAST_EMIT_FILE = ".fabric/.cache/maintenance-hint-last-emit";

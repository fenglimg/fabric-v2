# TASK-006 HUD 换皮(parity-safe)
- renderAiSink: sectionBar(Fabric Knowledge·N active·M reference)+ scopeBadge per line;仅用 lib/theme.cjs 基元,无 tui/structure import。renderAiSink/layerToScope 入 module.exports。theme-parity 绿,NO_COLOR snapshot 绿。

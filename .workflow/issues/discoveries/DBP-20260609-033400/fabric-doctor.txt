[error] fabric doctor C:\Project\fabric-v2
TL;DR (top 3 of 7, severity order: fixable→manual→warn):
  [error] bootstrap_snapshot_drift: .fabric/AGENTS.md 内容与 BOOTSTRAP_CANONICAL 逐字节不一致。
    → 运行 `fabric doctor --fix` 恢复 canonical bootstrap snapshot
  [warn] events_jsonl_health_degraded: .fabric/events.jsonl 含 647 行 metric-counter 类 event_type (knowledge_context_planned, knowledge_sections_fetched, knowledge_consumed)。这些 e...
    → 运行 `fabric doctor --fix` —— 它会触发 rotation 并 flush metrics.jsonl(rc.2 F16: 无需重启 server 即可清出 idle 期未刷的 metric counter)。若告警仍持续, 再重启 MCP server 让 startMetricsFlush + startRotationTick 重新调度。若 metric_leak 命中, 检查最近代码改动是否绕过 bumpCounter API 直接 appendEventLedgerEvent 写了 4 个 metric-managed event_type 之一。
  [warn] skill_token_budget_exceeded: 1 个 SKILL.md 超出 token budget: fabric-archive=5721 tok (warn)。建议把详细内容下沉到 ref/ progressive disclosure。
    → (maintainer-only remediation — 加 `fabric doctor --verbose` 查看)
[ok] Bootstrap anchor: repo root 下已存在 Bootstrap anchor：AGENTS.md, CLAUDE.md。
[ok] Bootstrap marker 迁移: bootstrap 目标文件中未检测到旧 fabric:knowledge-base marker。
[error] Bootstrap snapshot drift: .fabric/AGENTS.md 内容与 BOOTSTRAP_CANONICAL 逐字节不一致。
[ok] Managed block drift: Three-end managed blocks 与 expectedBody 逐字节一致。
[ok] Baseline 文件名格式: 所有 baseline knowledge 文件都使用 canonical `${id}--${slug}.md` 文件名格式。
[ok] Scan evidence: .fabric/forensic.json 对 unknown 有效。
[ok] Event ledger: .fabric/events.jsonl 已存在，可写，且可解析。
[ok] Event ledger partial write: events.jsonl 没有 partial trailing write。
[warn] Events ledger 健康 (rc.37 Plan B 5 hard gate): .fabric/events.jsonl 含 647 行 metric-counter 类 event_type (knowledge_context_planned, knowledge_sections_fetched, knowledge_consumed)。这些 event 应由 metrics.jsonl 计数, 不再进入 audit ledger。 | .fabric/metrics.jsonl 已 86 分钟未更新；server-side 60s flush 可能 stalled。
[ok] Event ledger schema 兼容性: events.jsonl 所有行都能解析为当前 schema。
[ok] Skill ref 镜像一致性: `.claude/skills/<slug>/ref/` 与 `.codex/skills/<slug>/ref/` 字节一致。
[warn] Skill token budget: 1 个 SKILL.md 超出 token budget: fabric-archive=5721 tok (warn)。建议把详细内容下沉到 ref/ progressive disclosure。
[warn] Skill description quality: 3 个 SKILL.md description 结构问题: fabric-archive: missing (no YAML frontmatter); fabric-review: missing (no YAML frontmatter); fabric-import: missing (no YAML frontmatter)。description 是 host 端 auto-invoke 的主要匹配信号。
[ok] Cite-policy Goodhart: 过去 7d 未检测到 cite-policy Goodhart 反模式。
[ok] Knowledge draft backlog: canonical knowledge entries 中 draft 占比正常 (< 50%, 或 workspace 太小不评)。
[ok] Knowledge auto-promote: 无 settled draft 待自动 promote (draft 均未满 14 天或已被 drift 标记)。
[ok] Knowledge tags coverage: canonical knowledge entries 中 empty tags 占比正常 (≤ 50%, 或 workspace 太小不评)。
[ok] Knowledge drift unconsumed: 近 30 天内 knowledge_drift_detected 事件已被对应 knowledge_demoted 消化,或事件数太少不评。
[ok] Claude MCP config 位置: mcpServers.fabric 不在 .claude/settings.json 中。
[ok] Stable ID collision: mounted store knowledge 中未发现已声明的 stable_id collisions。
[ok] Store counter drift: read-set 内每个 store 的 counters.json 都已 floor 到磁盘最大 stable_id。
[ok] Filesystem-edit fallback: No orphan canonical knowledge entries detected；events.jsonl promotion trail 完整。
[ok] Knowledge orphan demote: 没有 canonical knowledge entries 超过按 maturity 设定的 inactivity threshold。
[ok] Knowledge stale archive: 没有 draft knowledge entries 超过额外的 stale-archive quiet window。
[ok] Knowledge pending overdue: 没有 pending knowledge entries 超过 14-day review threshold。
[ok] Knowledge stable_id duplicate: team / personal trees 中没有 canonical knowledge files 共享 stable_id。
[ok] Knowledge layer mismatch: 所有 canonical knowledge files 都位于 stable_id prefix 声明的 layer 下。
[ok] Knowledge underseeded: 知识库已有 46 个 canonical entries（>= 10）。
[ok] Knowledge narrow without paths: 没有 narrow-scope canonical entries 的 relevance_paths array 为空。
[ok] Knowledge relevance_paths dangling: 所有 relevance_paths globs 都能在 workspace root 下解析到至少 1 个文件。
[ok] Knowledge relevance_paths drift: 所有 narrow-scope canonical entries 都至少有 1 个 relevance_path 在最近 90d 内被触碰。
[ok] Personal-layer path misclassify: 没有 personal-layer entries 的 relevance_paths 解析到当前项目内的文件。
[ok] Suspicious KB injection: 所有 canonical knowledge body 均未命中已知 prompt-injection 模式。
[ok] Knowledge narrow too few: Narrow-with-paths ratio 0%（0/0）；telemetry skipped（no edit-counter fires in window）。
[ok] Knowledge session-hints stale: .fabric/.cache/ 下没有超过 7 天的 session-hints cache files。
[ok] Serve lock: 未发现 .fabric/.serve.lock。
[ok] Knowledge relevance fields missing: 所有 pending entries 都声明了 relevance_scope 和 relevance_paths。
[ok] Skill markdown YAML: 所有 .claude/.codex SKILL.md frontmatter values 都能按 strict YAML 解析。
[ok] Onboard coverage: 尚未覆盖的 onboard slots：[tech-stack-decision, architecture-pattern, code-style-tone, build-system-idiom, domain-vocabulary]。0/5 filled；0 opted-out。
[ok] Claude Code hooks wired: .claude/settings.json 已注入 Stop:fabric-hint / SessionStart:knowledge-hint-broad / PreToolUse:knowledge-hint-narrow 三个 fabric hook。
[ok] Hooks 运行时健康: 已扫描 20 个 hook .cjs 文件，shebang 与 Node.js 语法解析全部通过。
[warn] Hooks 跨客户端内容一致性: 1 个 hook basename 在 client 之间内容 drift；首例：archive-hint.cjs（涉及 claude, codex）。`fabric install` 复制同一模板到三 client，drift 通常来自手动编辑。
[ok] 全局 fabric CLI 版本: PATH 上的 `fabric` 是 2.2.0-rc.5，与 rc.31+ 项目 schema 兼容。
[warn] 知识 summary 透明度: 26/46 个 entry (56.5%) 的 description.summary 等于 stable_id，超过 30% 阈值。narrow hint 输出会变成 `<id> · <id>` 而非真实概要，AI 看不到信息会主动跳过 fetch。首批不透明: team:KT-DEC-0009, team:KT-DEC-9001, team:KT-DEC-9002, team:KT-DEC-9003, team:KT-DEC-9004。
[warn] Store scope lint: 23 个 store scope 问题: 23 missing-scope。例如 missing_scope_fields — missing semantic_scope + visibility_store frontmatter (KP-DEC-9001)。
[ok] Promote ledger invariant: knowledge_proposed=3 ≥ knowledge_promote_started=1 ≥ knowledge_promoted=1，ledger 不变量持有。
[ok] Preexisting root markdown: project root 检测到 CLAUDE.md, AGENTS.md。这些 root files 不会被 Fabric MCP 自动加载。

可修复错误：
- bootstrap_snapshot_drift: .fabric/AGENTS.md 内容与 BOOTSTRAP_CANONICAL 逐字节不一致。
  → 运行 `fabric doctor --fix` 恢复 canonical bootstrap snapshot

警告：
- events_jsonl_health_degraded: .fabric/events.jsonl 含 647 行 metric-counter 类 event_type (knowledge_context_planned, knowledge_sections_fetched, knowledge_consumed)。这些 event 应由 metrics.jsonl 计数, 不再进入 audit ledger。 | .fabric/metrics.jsonl 已 86 分钟未更新；server-side 60s flush 可能 stalled。
  → 运行 `fabric doctor --fix` —— 它会触发 rotation 并 flush metrics.jsonl(rc.2 F16: 无需重启 server 即可清出 idle 期未刷的 metric counter)。若告警仍持续, 再重启 MCP server 让 startMetricsFlush + startRotationTick 重新调度。若 metric_leak 命中, 检查最近代码改动是否绕过 bumpCounter API 直接 appendEventLedgerEvent 写了 4 个 metric-managed event_type 之一。
- skill_token_budget_exceeded: 1 个 SKILL.md 超出 token budget: fabric-archive=5721 tok (warn)。建议把详细内容下沉到 ref/ progressive disclosure。
  → (maintainer-only remediation — 加 `fabric doctor --verbose` 查看)
- skill_description_quality: 3 个 SKILL.md description 结构问题: fabric-archive: missing (no YAML frontmatter); fabric-review: missing (no YAML frontmatter); fabric-import: missing (no YAML frontmatter)。description 是 host 端 auto-invoke 的主要匹配信号。
  → (maintainer-only remediation — 加 `fabric doctor --verbose` 查看)
- hooks_content_drift: 1 个 hook basename 在 client 之间内容 drift；首例：archive-hint.cjs（涉及 claude, codex）。`fabric install` 复制同一模板到三 client，drift 通常来自手动编辑。
  → 运行 `fabric install` 把所有 client 的 hook 副本恢复到 canonical 模板。若你确实需要 client-specific hook 行为，建议改 lib/ 共享脚本或 templates/hooks/configs/ 配置而非直接编辑安装后的 .cjs。
- knowledge_summary_opaque: 26/46 个 entry (56.5%) 的 description.summary 等于 stable_id，超过 30% 阈值。narrow hint 输出会变成 `<id> · <id>` 而非真实概要，AI 看不到信息会主动跳过 fetch。首批不透明: team:KT-DEC-0009, team:KT-DEC-9001, team:KT-DEC-9002, team:KT-DEC-9003, team:KT-DEC-9004。
  → 调 fabric-review skill 重写不透明 summary 为一句人类可读的概要。rc.35 hint renderer fallback (TASK-06) 也会从 entry 的 `## Summary` 段自动合成临时 summary。
- store_scope_lint: 23 个 store scope 问题: 23 missing-scope。例如 missing_scope_fields — missing semantic_scope + visibility_store frontmatter (KP-DEC-9001)。
  → 调 `fabric store backfill-scope` 补缺失的 semantic_scope/visibility_store;`fabric store re-scope` 修 dangling 的 project: 坐标;把 personal-scope 条目移出 shared store(personal 知识只存个人 store,R5#3)。

MCP payload 阈值：
- warn=16 KB, hard=64 KB (来源: default)

store health
[info] [personal, team] by-alias readability link(s) out of sync for personal, team; run `fabric doctor --fix` to repair ~/.fabric/stores/by-alias/

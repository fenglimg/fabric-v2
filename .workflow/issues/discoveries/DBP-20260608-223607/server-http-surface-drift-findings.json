{
  "id": "3",
  "perspective": "server-http-surface-drift",
  "result_status": "completed",
  "findings": "发现 5 个 server/HTTP surface drift：quarantine 恢复说明过期、dashboard copy 工具清单残留、workspace 声明矛盾、HTTP 测试头仍列已删 endpoint、Dashboard conventions 指向已删除包。",
  "issues_found": [
    {
      "title": "HTTP quarantine 恢复说明仍停留在 Part 1，指向已搬走的 mainline HTTP 实现",
      "severity": "medium",
      "description": "`packages/server-http-experimental/README.md` 仍说 HTTP server 还在 `packages/server/src/` 且 Part 2 未来才会搬迁；但 main server 入口注释已说明 `startHttpServer` 被 quarantine，serve-lock 已完全搬到 `packages/server-http-experimental/`，并且 experimental 包已有 `src/http.ts`。维护者按 README 恢复步骤会从错误位置 surgical merge。",
      "location": "packages/server-http-experimental/README.md:43",
      "fix_direction": "把 README 的 Part 1/Part 2 叙述改为当前状态：HTTP app、bearer-auth、serve-lock、integration tests 已在 experimental 包；恢复步骤应从 experimental 包迁回或重新注册，而不是从 `packages/server/src/http.ts` 取。同步修正 `packages/server/src/index.ts:381` 的 `./http.ts` 过期注释。",
      "affected_components": [
        "packages/server-http-experimental/README.md",
        "packages/server/src/index.ts",
        "HTTP quarantine restore path"
      ]
    },
    {
      "title": "Tooling manifest 仍登记已删除的 dashboard static copy build script",
      "severity": "medium",
      "description": "`docs/tooling-manifest.md` 和 machine-readable `docs/tooling-manifest.json` 仍把 `copy-dashboard-dist` 描述为 server build 的 dashboard asset bridge，路径是 `packages/server/scripts/copy-dashboard.mjs`；但当前 `packages/server/package.json` 的 build 只有 `tsup src/index.ts --format esm --dts --clean`，server 包也没有 scripts 目录。这会把维护者引向不存在的 Dashboard/HTTP build surface。",
      "location": "docs/tooling-manifest.md:22",
      "fix_direction": "删除或归档 `copy-dashboard-dist` manifest 条目；若保留为历史信息，明确标记为 quarantined/removed，并从 machine-readable manifest 中移出 active tools 列表。",
      "affected_components": [
        "docs/tooling-manifest.md",
        "docs/tooling-manifest.json",
        "packages/server/package.json",
        "Dashboard static serving docs"
      ]
    },
    {
      "title": "Root workspace 声明与 pnpm workspace 对 server-http-experimental 的 quarantine 边界不一致",
      "severity": "low",
      "description": "根 `package.json` 仍声明 npm-style `workspaces: [\"packages/*\"]`，会把 `packages/server-http-experimental` 纳入支持 package.json workspaces 的工具；但 `pnpm-workspace.yaml` 明确用 `!packages/server-http-experimental` 排除该包，并注释说 `pnpm -r build/test/tsc` 要跳过它。不同 workspace 消费者看到的 HTTP quarantine 边界不一致。",
      "location": "package.json:5",
      "fix_direction": "统一 workspace source of truth：要么移除 root `package.json` 的宽泛 workspaces 字段并只保留 `pnpm-workspace.yaml`，要么改成显式列表/排除策略，并在 README 中说明只有 pnpm workspace 定义有效。",
      "affected_components": [
        "package.json",
        "pnpm-workspace.yaml",
        "packages/server-http-experimental",
        "monorepo tooling"
      ]
    },
    {
      "title": "HTTP integration test header 仍把已删除的 `/api/intent/annotate` 列为 exercised endpoint",
      "severity": "low",
      "description": "`http-endpoints.test.ts` 文件头宣称测试会 exercise `POST /api/intent/annotate`，但同一文件后面明确写着该 endpoint 在 rc.5 A2 已移除且 orphan tests deleted。这个注释让 retired REST surface 看起来仍是当前测试契约的一部分。",
      "location": "packages/server-http-experimental/__tests__/integration/http-endpoints.test.ts:14",
      "fix_direction": "从文件头 endpoint 清单删除 `/api/intent/annotate`，或移动到 removed-history 小节并重新编号剩余 endpoint，避免测试契约与移除说明互相矛盾。",
      "affected_components": [
        "packages/server-http-experimental/__tests__/integration/http-endpoints.test.ts",
        "REST endpoint tests",
        "HTTP quarantine docs"
      ]
    },
    {
      "title": "Conventions 仍把已删除 `packages/dashboard` 当作现行 UI token source",
      "severity": "low",
      "description": "`docs/CONVENTIONS.md` 要求 Dashboard 视觉 token 以 `packages/dashboard/src/styles/tokens.css` 为准，并保留 Dashboard 观察优先约束；但 `docs/CODEBASE_LANDSCAPE.md` 已说明 `packages/dashboard` 在 v2.0.0-rc.37 随 HTTP/REST/SSE quarantine 后不再存在于主线 packages。UI/HTTP 恢复前，这条 convention 指向已删除主线包。",
      "location": "docs/CONVENTIONS.md:59",
      "fix_direction": "将 Dashboard 相关 convention 标记为 quarantined historical reference，改指 `packages/server-http-experimental` 中的历史素材或删除 active 约束；未来恢复 Web UI 时再建立新的 token source。",
      "affected_components": [
        "docs/CONVENTIONS.md",
        "docs/CODEBASE_LANDSCAPE.md",
        "Dashboard quarantine docs"
      ]
    }
  ],
  "severity_distribution": {
    "critical": 0,
    "high": 0,
    "medium": 2,
    "low": 3
  },
  "error": ""
}
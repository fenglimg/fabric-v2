{
  "id": "5",
  "perspective": "schema-issue-metadata-contract-drift",
  "result_status": "completed",
  "findings": "发现 3 个 schema/metadata 合约漂移问题：issue source 值混用、store counters 文档仍指向 retired agents.meta 位置、active_project 未按 scope/project-id 规则校验却被写入 semantic_scope。",
  "issues_found": [
    {
      "title": "Issue registry mixes source=discovery and source=discover",
      "severity": "medium",
      "description": "现有 .workflow/issues/issues.jsonl 同时持久化旧记录 source: \"discovery\" 和新记录 source: \"discover\"，例如第 1 行与第 188 行。按 source 过滤、去重或迁移时会把同一 discovery 来源拆成两个来源，且没有看到该文件内有迁移归一。",
      "location": ".workflow/issues/issues.jsonl:1",
      "fix_direction": "确定唯一 canonical source 值，迁移历史 JSONL 记录，并让 discovery/dedup 写入路径和查询过滤统一使用同一个枚举；如需兼容旧值，应在读路径做 alias 归一并加回归测试。",
      "affected_components": [
        "issue registry",
        "manage-issue discovery",
        "issues.jsonl queries"
      ]
    },
    {
      "title": "Counter storage documentation still describes retired agents.meta counters",
      "severity": "low",
      "description": "docs/data-schema.md 仍说 counters 存在每个 meta file 的 counters key 下；但 store-counters.ts 明确说明 <projectRoot>/.fabric/agents.meta.json#counters 已 retired，stable_id counter 已移动到 store 根的 counters.json，store.ts 也把 countersFile 定义为 counters.json。该文档会误导恢复/迁移操作去修 agents.meta 而不是 store counters ledger。",
      "location": "docs/data-schema.md:163",
      "fix_direction": "更新 docs/data-schema.md 的 counters envelope 章节：把存储位置改为每个 store 根的 counters.json，说明 AgentsMetaCountersSchema 仅作为 envelope shape 复用，并链接 reconcileStoreCounters/store-counters.ts。",
      "affected_components": [
        "docs",
        "store counters",
        "stable_id allocation",
        "migration/recovery docs"
      ]
    },
    {
      "title": "active_project config accepts values that violate semantic_scope/project id contract",
      "severity": "medium",
      "description": "fabric-config.ts 将 active_project 定义为任意 z.string()，但 cross-store-write.ts 会直接拼成 semantic_scope: project:${activeProject}；scope.ts 要求 scope coordinate 只能是小写 [a-z0-9_-] 分段，store.ts 也要求 store project id 是同样的小写单段。手工配置或坏写入可通过 config schema，随后写出 schema-invalid semantic_scope 并破坏 project filter/doctor lint 的一致性。",
      "location": "packages/shared/src/schemas/fabric-config.ts:98",
      "fix_direction": "把 active_project schema 收紧为 STORE_PROJECT_ID_PATTERN 或复用单段 scope schema；在 resolveWriteScopeMeta 写出前用 scopeCoordinateSchema 校验 project:${activeProject}，并为手工 invalid active_project 配置添加拒绝/诊断测试。",
      "affected_components": [
        "fabric-config schema",
        "cross-store write",
        "semantic_scope metadata",
        "project binding"
      ]
    }
  ],
  "severity_distribution": {
    "critical": 0,
    "high": 0,
    "medium": 2,
    "low": 1
  },
  "error": ""
}
{
  "id": "TASK-035",
  "title": "Init scaffold raw → atomic (4 callsites)",
  "description": "Per stability-deepdive inventory: init.ts:570/573/576/583 are 4 P1 raw fs.write callsites for bootstrap README / agents.meta.json initial / INITIAL_TAXONOMY.md / forensic.json. Lift to atomicWriteText/atomicWriteJson via shared. (init.ts:580 events.jsonl 0-byte create stays raw — atomic by definition.)",
  "scope": "Layer 2 P1 — init scaffold durability",
  "files": [
    { "path": "packages/cli/src/commands/init.ts", "change": "Lines 570/573/576/583: replace raw fs.writeFile with atomicWriteText/atomicWriteJson from shared. Line 580 (events.jsonl 0-byte create) stays raw." },
    { "path": "packages/cli/__tests__/init-atomic.test.ts", "change": "NEW: assert (a) crash mid-write leaves no half-written README; (b) all 4 callsites use atomic; (c) line 580 stays raw 0-byte create" }
  ],
  "convergence": {
    "criteria": [
      "All 4 P1 init writes use atomic",
      "events.jsonl 0-byte create still works",
      "Tests pass; init flow integration tests pass"
    ]
  },
  "depends_on": ["TASK-001"],
  "metadata": {
    "layer": 2,
    "release_tag": "1.8.0",
    "recommendation_id": "R7.5",
    "branch": "release/v1.8.0-stabilization",
    "priority": "P1",
    "estimated_loc": 20
  }
}

# Issue Discovery Report

## Summary
- Session: DBP-20260608-215329
- Mode: multi
- Perspectives: 8
- Raw findings: 16
- Unique issues: 15

## Breakdown by Perspective
| Perspective | Findings | Critical | High | Medium | Low |
|-------------|----------|----------|------|--------|-----|
| security | 1 | 0 | 0 | 1 | 0 |
| performance | 1 | 0 | 1 | 0 | 0 |
| reliability | 3 | 0 | 2 | 1 | 0 |
| maintainability | 2 | 0 | 0 | 2 | 0 |
| scalability | 1 | 0 | 1 | 0 | 0 |
| ux | 2 | 0 | 0 | 2 | 0 |
| accessibility | 3 | 0 | 0 | 2 | 1 |
| compliance | 3 | 0 | 0 | 3 | 0 |

## Severity Distribution
| Severity | Count |
|----------|-------|
| Critical | 0 |
| High | 3 |
| Medium | 11 |
| Low | 1 |

## Perspective Details
### security
@{id=1; title=Security Scan; description=Scan codebase for security vulnerabilities: authentication bypass, injection flaws, command injection, path traversal, sensitive data exposure, insecure crypto, secrets in code, unsafe filesystem/process use. Rate each finding critical/high/medium/low with file:line references.; perspective=security; scope_glob=packages/**/*.{ts,tsx,js,jsx,cjs,mjs};scripts/**/*.{ts,js,mjs,cjs};.github/**/*.{yml,yaml};docs/**/*.md; deps=; context_from=; wave=1; status=completed; findings=Security scan found one concrete medium issue: mounted store aliases are only non-empty strings but are used directly as by-alias filesystem path segments, allowing path traversal outside the intended symlink directory.; issues_found=[{"title":"Store alias path traversal escapes by-alias symlink directory","severity":"medium","description":"Mounted store aliases are validated only as non-empty strings, but syncStoreAliasLinks and detectAliasLinkDrift use alias directly in join(byAliasDir, alias). An alias containing '../' or path separators can make doctor/store operations create, remove, or inspect symlinks outside ~/.fabric/stores/by-alias, causing unsafe filesystem mutation in the user's Fabric home.","location":"packages/shared/src/schemas/store.ts:210","fix_direction":"Constrain MountedStore.alias and storeIdentity.canonical_alias to a safe single path segment such as /^[A-Za-z0-9._-]+$/ and reject '.', '..', slash and backslash; also defensively resolve link paths and verify they remain inside byAliasDir before rmSync/symlinkSync/lstatSync.","affected_components":["packages/shared","packages/cli store operations","global store registry"]}]; severity_distribution={"critical":0,"high":0,"medium":1,"low":0}; error=}.findings

**Top Issues:**
- [medium] Store alias path traversal escapes by-alias symlink directory ($(@{title=Store alias path traversal escapes by-alias symlink directory; severity=medium; description=Mounted store aliases are validated only as non-empty strings, but syncStoreAliasLinks and detectAliasLinkDrift use alias directly in join(byAliasDir, alias). An alias containing '../' or path separators can make doctor/store operations create, remove, or inspect symlinks outside ~/.fabric/stores/by-alias, causing unsafe filesystem mutation in the user's Fabric home.; location=packages/shared/src/schemas/store.ts:210; fix_direction=Constrain MountedStore.alias and storeIdentity.canonical_alias to a safe single path segment such as /^[A-Za-z0-9._-]+$/ and reject '.', '..', slash and backslash; also defensively resolve link paths and verify they remain inside byAliasDir before rmSync/symlinkSync/lstatSync.; affected_components=System.Object[]}.location))

### performance
@{id=2; title=Performance Scan; description=Scan codebase for performance issues: repeated full scans, unbounded loops, missing caching, memory leaks, large payloads, blocking operations, unoptimized algorithms, excessive process spawning.; perspective=performance; scope_glob=packages/**/*.{ts,tsx,js,jsx,cjs,mjs};scripts/**/*.{ts,js,mjs,cjs}; deps=; context_from=; wave=1; status=completed; findings=Found one high-signal performance issue: the store-era recall path repeatedly walks and reads the full read-set store corpus during a single recall/section fetch.; issues_found=[{"title":"Store-era recall recomputes and rereads the full read-set corpus multiple times per request","severity":"high","description":"The hot recall path computes the store corpus revision, then separately builds raw candidates, and get_sections repeats the revision plus body-index build. All four calls funnel through walkReadSetStores(), which walks readKnowledgeAcrossStores() and readFileSyncs every store markdown file, so a normal fab_recall -> fab_get_knowledge_sections round trip can perform four full read-set walks/full-file reads before reading selected bodies again. Cost scales with total mounted store corpus size on every request despite the code already having the in-memory selection token and corpus revision opportunities for reuse.","location":"packages/server/src/services/cross-store-recall.ts:120","fix_direction":"Build the read-set corpus once per request (or cache it by store directory fingerprint/max mtime) and thread the entries/revision/body index through plan-context and knowledge-sections; at minimum derive computeReadSetRevision, raw items, and body index from a shared walk result so each store file is read once.","affected_components":["packages/server/src/services/cross-store-recall.ts","packages/server/src/services/plan-context.ts","packages/server/src/services/knowledge-sections.ts"]}]; severity_distribution={"critical":0,"high":1,"medium":0,"low":0}; error=}.findings

**Top Issues:**
- [high] Store-era recall recomputes and rereads the full read-set corpus multiple times per request ($(@{title=Store-era recall recomputes and rereads the full read-set corpus multiple times per request; severity=high; description=The hot recall path computes the store corpus revision, then separately builds raw candidates, and get_sections repeats the revision plus body-index build. All four calls funnel through walkReadSetStores(), which walks readKnowledgeAcrossStores() and readFileSyncs every store markdown file, so a normal fab_recall -> fab_get_knowledge_sections round trip can perform four full read-set walks/full-file reads before reading selected bodies again. Cost scales with total mounted store corpus size on every request despite the code already having the in-memory selection token and corpus revision opportunities for reuse.; location=packages/server/src/services/cross-store-recall.ts:120; fix_direction=Build the read-set corpus once per request (or cache it by store directory fingerprint/max mtime) and thread the entries/revision/body index through plan-context and knowledge-sections; at minimum derive computeReadSetRevision, raw items, and body index from a shared walk result so each store file is read once.; affected_components=System.Object[]}.location))

### reliability
@{id=3; title=Reliability Scan; description=Scan codebase for reliability issues: unhandled errors, missing retry/recovery, race conditions, data integrity gaps, missing graceful degradation, silent failures, atomicity issues.; perspective=reliability; scope_glob=packages/**/*.{ts,tsx,js,jsx,cjs,mjs};scripts/**/*.{ts,js,mjs,cjs}; deps=; context_from=; wave=1; status=completed; findings=发现 3 个高信号 reliability 问题：store counters 在损坏/迁移中会 fail-open 导致重复 stable_id，store/global/project config 的读改写缺少锁会丢失并发更新。; issues_found=[{"title":"Corrupt store counters fail open to zero and can re-mint existing stable_ids","severity":"high","description":"readStoreCounters returns default zero counters when counters.json is unreadable, invalid JSON, or schema-invalid, and allocateStoreKnowledgeId immediately uses that value inside the lock before writing nextCounters. If counters.json is corrupted while knowledge files with higher IDs still exist, the next approval can mint KT/KP IDs that already exist, corrupting cite identity and file naming invariants.","location":"packages/shared/src/store/store-counters.ts:48","fix_direction":"Make allocation fail closed on corrupt counters.json, preserve a .corrupted sidecar, and require/perform reconcileStoreCounters before minting. Only return zero counters for an absent file, not for parse/schema corruption on the write path.","affected_components":["store counters","stable_id allocation","fab_review approval"]},{"title":"store migrate removes sources before counters are reconciled, leaving an interruption window for duplicate IDs","severity":"high","description":"store-migrate writes imported entries into target stores, then removes the project sources, and only afterwards calls reconcileStoreCounters. Any crash, process kill, disk error, or interrupted command between the target writes/source removal and counter reconciliation leaves the store containing imported stable_ids while counters.json remains stale, so the next allocateStoreKnowledgeId can re-mint those IDs.","location":"packages/cli/src/store/store-migrate.ts:323","fix_direction":"Make migration transactional: reconcile/atomically write counters for each target before committing/removing sources, or write a migration receipt and recover/reconcile on the next run before any allocation. Use atomic writes and fail closed if counter reconciliation cannot complete.","affected_components":["fabric store migrate","store counters","knowledge migration"]},{"title":"Global/project store config mutations use unlocked read-modify-write and can lose concurrent updates","severity":"medium","description":"storeAdd/storeCreate/storeRemove and storeBind/storeSwitchWrite load config, mutate in memory, and save with plain writeFileSync through saveGlobalConfig/saveProjectConfig. Two concurrent CLI operations against the same global or project config can both read the same base state and the later save overwrites the earlier mount/bind/write-target update; interruption during write can also leave a truncated config that later loadGlobalConfig/loadProjectConfig parses without recovery.","location":"packages/cli/src/store/store-ops.ts:169","fix_direction":"Wrap global/project config read-modify-write operations in withFileLock keyed by the config path and persist via atomicWriteJson with a corruption sidecar/recovery path. Consider helper APIs that combine load-mutate-save under one lock.","affected_components":["fabric store add/remove/create","fabric store bind","fabric store switch-write","global/project config IO"]}]; severity_distribution={"critical":0,"high":2,"medium":1,"low":0}; error=}.findings

**Top Issues:**
- [high] Corrupt store counters fail open to zero and can re-mint existing stable_ids ($(@{title=Corrupt store counters fail open to zero and can re-mint existing stable_ids; severity=high; description=readStoreCounters returns default zero counters when counters.json is unreadable, invalid JSON, or schema-invalid, and allocateStoreKnowledgeId immediately uses that value inside the lock before writing nextCounters. If counters.json is corrupted while knowledge files with higher IDs still exist, the next approval can mint KT/KP IDs that already exist, corrupting cite identity and file naming invariants.; location=packages/shared/src/store/store-counters.ts:48; fix_direction=Make allocation fail closed on corrupt counters.json, preserve a .corrupted sidecar, and require/perform reconcileStoreCounters before minting. Only return zero counters for an absent file, not for parse/schema corruption on the write path.; affected_components=System.Object[]}.location))
- [high] store migrate removes sources before counters are reconciled, leaving an interruption window for duplicate IDs ($(@{title=store migrate removes sources before counters are reconciled, leaving an interruption window for duplicate IDs; severity=high; description=store-migrate writes imported entries into target stores, then removes the project sources, and only afterwards calls reconcileStoreCounters. Any crash, process kill, disk error, or interrupted command between the target writes/source removal and counter reconciliation leaves the store containing imported stable_ids while counters.json remains stale, so the next allocateStoreKnowledgeId can re-mint those IDs.; location=packages/cli/src/store/store-migrate.ts:323; fix_direction=Make migration transactional: reconcile/atomically write counters for each target before committing/removing sources, or write a migration receipt and recover/reconcile on the next run before any allocation. Use atomic writes and fail closed if counter reconciliation cannot complete.; affected_components=System.Object[]}.location))
- [medium] Global/project store config mutations use unlocked read-modify-write and can lose concurrent updates ($(@{title=Global/project store config mutations use unlocked read-modify-write and can lose concurrent updates; severity=medium; description=storeAdd/storeCreate/storeRemove and storeBind/storeSwitchWrite load config, mutate in memory, and save with plain writeFileSync through saveGlobalConfig/saveProjectConfig. Two concurrent CLI operations against the same global or project config can both read the same base state and the later save overwrites the earlier mount/bind/write-target update; interruption during write can also leave a truncated config that later loadGlobalConfig/loadProjectConfig parses without recovery.; location=packages/cli/src/store/store-ops.ts:169; fix_direction=Wrap global/project config read-modify-write operations in withFileLock keyed by the config path and persist via atomicWriteJson with a corruption sidecar/recovery path. Consider helper APIs that combine load-mutate-save under one lock.; affected_components=System.Object[]}.location))

### maintainability
@{id=4; title=Maintainability Scan; description=Scan codebase for maintainability issues: code duplication, tight coupling, missing abstractions, unclear naming, dead code, overly complex functions, stale docs that mislead implementation.; perspective=maintainability; scope_glob=packages/**/*.{ts,tsx,js,jsx,cjs,mjs};docs/**/*.md;scripts/**/*.{ts,js,mjs,cjs}; deps=; context_from=; wave=1; status=completed; findings=Found 2 maintainability issues: retired install implementation still coexists with the live install-v2 entry, and generated client hook copies are checked into the package alongside templates.; issues_found=[{"title":"Retired install implementation remains beside live install-v2 and is still tested","severity":"medium","description":"The command registry routes production `fabric install` to `./install-v2.js`, but the older `packages/cli/src/commands/install.ts` still exports a full `installCommand` and `runInitCommand` implementation with the same command metadata. Tests still import the retired module directly, so behavior can diverge between tested legacy code and the shipped command path, making maintenance and regression triage ambiguous.","location":"packages/cli/src/commands/index.ts:8","fix_direction":"Either delete/quarantine `commands/install.ts` and migrate the remaining tests/helpers to `install-v2` or shared install pipeline modules, or make `install.ts` a thin compatibility re-export of the live implementation so there is only one install behavior to maintain.","affected_components":["packages/cli/src/commands","packages/cli/src/install/pipeline","packages/cli/__tests__"]},{"title":"Client hook scripts are checked in as four identical source copies","severity":"medium","description":"The package contains the template hook plus identical `.claude`, `.codex`, and `.cursor` hook copies; for example `fabric-hint.cjs` is present under `templates/hooks/` and all three client directories with identical hashes. The installer also copies templates into those client paths, so checked-in generated copies create a large duplication surface where hook changes can drift or require unnecessary multi-file edits.","location":"packages/cli/src/install/skills-and-hooks.ts:187","fix_direction":"Keep `packages/cli/templates/hooks/**` as the only source of truth and generate/copy client hook directories during install or tests; if fixture copies are needed, move them under explicit test fixtures and add a drift check rather than treating them as maintained source files.","affected_components":["packages/cli/templates/hooks","packages/cli/.claude/hooks","packages/cli/.codex/hooks","packages/cli/.cursor/hooks","packages/cli/src/install/skills-and-hooks.ts"]}]; severity_distribution={"critical":0,"high":0,"medium":2,"low":0}; error=}.findings

**Top Issues:**
- [medium] Retired install implementation remains beside live install-v2 and is still tested ($(@{title=Retired install implementation remains beside live install-v2 and is still tested; severity=medium; description=The command registry routes production `fabric install` to `./install-v2.js`, but the older `packages/cli/src/commands/install.ts` still exports a full `installCommand` and `runInitCommand` implementation with the same command metadata. Tests still import the retired module directly, so behavior can diverge between tested legacy code and the shipped command path, making maintenance and regression triage ambiguous.; location=packages/cli/src/commands/index.ts:8; fix_direction=Either delete/quarantine `commands/install.ts` and migrate the remaining tests/helpers to `install-v2` or shared install pipeline modules, or make `install.ts` a thin compatibility re-export of the live implementation so there is only one install behavior to maintain.; affected_components=System.Object[]}.location))
- [medium] Client hook scripts are checked in as four identical source copies ($(@{title=Client hook scripts are checked in as four identical source copies; severity=medium; description=The package contains the template hook plus identical `.claude`, `.codex`, and `.cursor` hook copies; for example `fabric-hint.cjs` is present under `templates/hooks/` and all three client directories with identical hashes. The installer also copies templates into those client paths, so checked-in generated copies create a large duplication surface where hook changes can drift or require unnecessary multi-file edits.; location=packages/cli/src/install/skills-and-hooks.ts:187; fix_direction=Keep `packages/cli/templates/hooks/**` as the only source of truth and generate/copy client hook directories during install or tests; if fixture copies are needed, move them under explicit test fixtures and add a drift check rather than treating them as maintained source files.; affected_components=System.Object[]}.location))

### scalability
@{id=5; title=Scalability Scan; description=Scan codebase for scalability issues: hardcoded limits, single-thread bottlenecks, stateful assumptions, schema rigidity, missing pagination, O(N) startup/session work that grows with knowledge stores.; perspective=scalability; scope_glob=packages/**/*.{ts,tsx,js,jsx,cjs,mjs};scripts/**/*.{ts,js,mjs,cjs};docs/**/*.md; deps=; context_from=; wave=1; status=completed; findings=Found one new high-severity scalability issue: v2.2 store-only recall re-walks and re-reads every read-set store multiple times per fab_recall; one review-search O(N) candidate was skipped as an existing issue.; issues_found=[{"title":"Store-only recall re-walks and re-reads every read-set store multiple times per request","severity":"high","description":"walkReadSetStores enumerates all read-set stores via readKnowledgeAcrossStores and readFileSyncs every markdown file; planContext calls computeReadSetRevision and buildCrossStoreRawItems, then fab_recall calls getKnowledgeSections, which calls computeReadSetRevision and buildCrossStoreBodyIndex again. A normal fab_recall therefore pays roughly four full store walks/reads before reading selected bodies, so latency scales linearly with total mounted-store corpus size and is multiplied per request.","location":"packages/server/src/services/cross-store-recall.ts:95","fix_direction":"Introduce a per-request StoreCorpus snapshot containing entries, revision, raw candidates, and id-to-body refs; compute it once in planContext/recall and pass it through to getKnowledgeSections, or memoize by read-set plus directory fingerprint/mtime so unchanged stores are not re-read and re-hashed repeatedly.","affected_components":["fab_recall","fab_plan_context","fab_get_knowledge_sections","cross-store recall","multi-store read path"]}]; severity_distribution={"critical":0,"high":1,"medium":0,"low":0}; error=}.findings

**Top Issues:**
- [high] Store-only recall re-walks and re-reads every read-set store multiple times per request ($(@{title=Store-only recall re-walks and re-reads every read-set store multiple times per request; severity=high; description=walkReadSetStores enumerates all read-set stores via readKnowledgeAcrossStores and readFileSyncs every markdown file; planContext calls computeReadSetRevision and buildCrossStoreRawItems, then fab_recall calls getKnowledgeSections, which calls computeReadSetRevision and buildCrossStoreBodyIndex again. A normal fab_recall therefore pays roughly four full store walks/reads before reading selected bodies, so latency scales linearly with total mounted-store corpus size and is multiplied per request.; location=packages/server/src/services/cross-store-recall.ts:95; fix_direction=Introduce a per-request StoreCorpus snapshot containing entries, revision, raw candidates, and id-to-body refs; compute it once in planContext/recall and pass it through to getKnowledgeSections, or memoize by read-set plus directory fingerprint/mtime so unchanged stores are not re-read and re-hashed repeatedly.; affected_components=System.Object[]}.location))

### ux
@{id=6; title=UX Scan; description=Scan codebase for UX issues: confusing CLI flows, missing user feedback, inconsistent behavior, missing loading states, poor error messages, docs/commands teaching wrong remediation.; perspective=ux; scope_glob=packages/**/*.{ts,tsx,js,jsx,cjs,mjs};docs/**/*.md;README.md; deps=; context_from=; wave=1; status=completed; findings=Found 2 concrete UX issues: install-v2 can duplicate semantic-search guidance/prompts, and `fabric info scope` without an argument reaches runtime scope resolution instead of usage validation.; issues_found=[{"title":"install-v2 runs semantic-search guidance twice after successful pipeline install","severity":"medium","description":"The new install pipeline includes GuidanceStage, whose documented responsibility includes handling semantic search and whose execute path prompts/enables it. After pipeline success, runInitCommand preserves the old post-install semantic-search block and calls the same prompt/enabling logic again. In an interactive install this can ask the same question twice; with --enable-embed it can print duplicate enablement guidance in one command run.","location":"packages/cli/src/install/pipeline/guidance.stage.ts:58","fix_direction":"Move semantic-search onboarding to a single owner. Prefer keeping it inside GuidanceStage and deleting the legacy post-install block in install-v2, or make GuidanceStage purely presentational and keep all mutation prompts after the pipeline.","affected_components":["fabric install","install-v2 pipeline","GuidanceStage","semantic search onboarding"]},{"title":"`fabric info scope` without scope argument falls through to runtime crash","severity":"medium","description":"The `scope` positional is optional, but the scope branch calls `runScopeExplain(args.scope!)`. The scope validator uses RegExp.test(scope), and the shared regex accepts the coerced string `undefined`; the built dist then reaches resolver code and throws a raw TypeError stack trace instead of showing usage or an actionable missing-argument error.","location":"packages/cli/src/commands/info.ts:58","fix_direction":"Require the scope positional when subcommand is `scope`, or explicitly guard in resolveMode/runScopeExplain and render `fabric info scope <scope>` usage with a nonzero exit before calling scopeExplain.","affected_components":["fabric info","scope-explain UX","CLI argument validation"]}]; severity_distribution={"critical":0,"high":0,"medium":2,"low":0}; error=}.findings

**Top Issues:**
- [medium] install-v2 runs semantic-search guidance twice after successful pipeline install ($(@{title=install-v2 runs semantic-search guidance twice after successful pipeline install; severity=medium; description=The new install pipeline includes GuidanceStage, whose documented responsibility includes handling semantic search and whose execute path prompts/enables it. After pipeline success, runInitCommand preserves the old post-install semantic-search block and calls the same prompt/enabling logic again. In an interactive install this can ask the same question twice; with --enable-embed it can print duplicate enablement guidance in one command run.; location=packages/cli/src/install/pipeline/guidance.stage.ts:58; fix_direction=Move semantic-search onboarding to a single owner. Prefer keeping it inside GuidanceStage and deleting the legacy post-install block in install-v2, or make GuidanceStage purely presentational and keep all mutation prompts after the pipeline.; affected_components=System.Object[]}.location))
- [medium] `fabric info scope` without scope argument falls through to runtime crash ($(@{title=`fabric info scope` without scope argument falls through to runtime crash; severity=medium; description=The `scope` positional is optional, but the scope branch calls `runScopeExplain(args.scope!)`. The scope validator uses RegExp.test(scope), and the shared regex accepts the coerced string `undefined`; the built dist then reaches resolver code and throws a raw TypeError stack trace instead of showing usage or an actionable missing-argument error.; location=packages/cli/src/commands/info.ts:58; fix_direction=Require the scope positional when subcommand is `scope`, or explicitly guard in resolveMode/runScopeExplain and render `fabric info scope <scope>` usage with a nonzero exit before calling scopeExplain.; affected_components=System.Object[]}.location))

### accessibility
@{id=7; title=Accessibility Scan; description=Scan codebase for accessibility issues: keyboard navigation gaps, screen reader issues in TUI, color-only status, focus management issues, inaccessible docs/examples.; perspective=accessibility; scope_glob=packages/cli/src/tui/**/*.{ts,tsx};packages/cli/src/**/*.{ts,tsx};docs/**/*.md;README.md; deps=; context_from=; wave=1; status=completed; findings=Found 3 concrete TUI accessibility gaps: status is conveyed through color/glyphs, running steps use an always-animating Braille spinner, and the exported color-disable renderer config is not honored.; issues_found=[{"title":"Ink TUI status states rely on color and Unicode glyphs instead of spoken text labels","severity":"medium","description":"Status rendering hard-codes color maps and symbol maps, then renders only the glyph as the status cue; running/success/error/skipped state is not emitted as text for screen readers or color-blind users. This affects install pipeline step output and TUI status messages, separate from the shared colors.ts fallback path.","location":"packages/cli/src/tui/StepCounter.tsx:28","fix_direction":"Render textual state labels such as [running], [ok], [warn], [error], [skipped] alongside glyphs in all TUI modes, or route TUI status indicators through the shared accessible symbol/label helpers.","affected_components":["packages/cli/src/tui","install pipeline TUI"]},{"title":"Install TUI spinner animates Braille frames continuously with no reduced-motion or screen-reader-safe mode","severity":"medium","description":"Spinner defines Braille frames and advances them every 80ms, and the install pipeline renders this spinner for every running stage. There is no config or environment check to switch to static progress text for screen-reader users or reduced-motion contexts.","location":"packages/cli/src/tui/Spinner.tsx:12","fix_direction":"Add a reduced-motion/text-only mode that renders a stable 'Running: <stage>' line, and have the install pipeline or renderer choose it when color/animation is disabled or when an explicit config/env option requests accessible output.","affected_components":["packages/cli/src/tui","packages/cli/src/install/pipeline"]},{"title":"OutputRendererConfig.colors is declared but InkOutputRenderer never applies it","severity":"low","description":"The public renderer config exposes colors?: boolean, but InkOutputRenderer only stores the config and renderComponent calls Ink render(node) without passing or propagating color preferences; child components hard-code Text color props. A caller cannot force text-only/no-color TUI output through this config.","location":"packages/cli/src/tui/InkOutputRenderer.ts:34","fix_direction":"Plumb the color flag into TUI components or renderer context, suppress Ink color props when false, and add tests proving createInkRenderer({ colors: false }) produces text-only output.","affected_components":["packages/cli/src/tui"]}]; severity_distribution={"critical":0,"high":0,"medium":2,"low":1}; error=}.findings

**Top Issues:**
- [medium] Ink TUI status states rely on color and Unicode glyphs instead of spoken text labels ($(@{title=Ink TUI status states rely on color and Unicode glyphs instead of spoken text labels; severity=medium; description=Status rendering hard-codes color maps and symbol maps, then renders only the glyph as the status cue; running/success/error/skipped state is not emitted as text for screen readers or color-blind users. This affects install pipeline step output and TUI status messages, separate from the shared colors.ts fallback path.; location=packages/cli/src/tui/StepCounter.tsx:28; fix_direction=Render textual state labels such as [running], [ok], [warn], [error], [skipped] alongside glyphs in all TUI modes, or route TUI status indicators through the shared accessible symbol/label helpers.; affected_components=System.Object[]}.location))
- [medium] Install TUI spinner animates Braille frames continuously with no reduced-motion or screen-reader-safe mode ($(@{title=Install TUI spinner animates Braille frames continuously with no reduced-motion or screen-reader-safe mode; severity=medium; description=Spinner defines Braille frames and advances them every 80ms, and the install pipeline renders this spinner for every running stage. There is no config or environment check to switch to static progress text for screen-reader users or reduced-motion contexts.; location=packages/cli/src/tui/Spinner.tsx:12; fix_direction=Add a reduced-motion/text-only mode that renders a stable 'Running: <stage>' line, and have the install pipeline or renderer choose it when color/animation is disabled or when an explicit config/env option requests accessible output.; affected_components=System.Object[]}.location))
- [low] OutputRendererConfig.colors is declared but InkOutputRenderer never applies it ($(@{title=OutputRendererConfig.colors is declared but InkOutputRenderer never applies it; severity=low; description=The public renderer config exposes colors?: boolean, but InkOutputRenderer only stores the config and renderComponent calls Ink render(node) without passing or propagating color preferences; child components hard-code Text color props. A caller cannot force text-only/no-color TUI output through this config.; location=packages/cli/src/tui/InkOutputRenderer.ts:34; fix_direction=Plumb the color flag into TUI components or renderer context, suppress Ink color props when false, and add tests proving createInkRenderer({ colors: false }) produces text-only output.; affected_components=System.Object[]}.location))

### compliance
@{id=8; title=Compliance Scan; description=Scan codebase for compliance issues: audit trail gaps, source attribution gaps, privacy/PII exposure, unsafe retention, missing provenance for knowledge writes, regulatory/policy drift.; perspective=compliance; scope_glob=packages/**/*.{ts,tsx,js,jsx,cjs,mjs};docs/**/*.md;AGENTS.md; deps=; context_from=; wave=1; status=completed; findings=Found 3 concrete compliance issues: raw session digest retention, missing session/actor provenance on review lifecycle events, and an archive secret-scan gap for persisted path metadata.; issues_found=[{"title":"Session digests persist raw user transcript snippets without redaction or retention controls","severity":"medium","description":"Stop-hook transcript summarization pushes raw user message text into user_messages, then session-digest-writer writes up to 10 messages of 500 chars each under .fabric/.cache/session-digests. The writer only truncates; it does not call the existing secret/PII redaction utilities and no TTL cleanup is configured for these digest files, so sensitive prompts can linger in local cache and later feed fabric-archive context.","location":"packages/cli/templates/hooks/lib/session-digest-writer.cjs:71","fix_direction":"Run digest content through the shared redactSecrets/privacy filter before write, add a configurable TTL/cleanup for .fabric/.cache/session-digests, and prefer summaries over verbatim user-message bullets.","affected_components":["hooks","session digest cache","fabric-archive"]},{"title":"fab_review lifecycle events lack invoker session provenance","severity":"medium","description":"FabReviewInputSchema branches for approve/reject/defer contain only pending paths/reason/until and no session_id or actor field; the service then emits knowledge_proposed, knowledge_promote_started, knowledge_promoted, knowledge_rejected, and knowledge_deferred events with timestamp/reason but no session_id/correlation_id. The ledger records that a knowledge write happened but cannot attribute it to the invoking session or operator.","location":"packages/shared/src/schemas/api-contracts.ts:907","fix_direction":"Add optional session_id/correlation_id (and, if available, actor/client) to fab_review input, thread it through reviewKnowledge, and include it on all review mutation events; add tests asserting approve/reject/defer/promote events preserve provenance.","affected_components":["fab_review","event ledger","knowledge review"]},{"title":"Archive secret scan omits persisted path metadata fields","severity":"medium","description":"extractKnowledge gates secret-shaped content by scanning user_messages_summary, session_context, must_read_if, and intent_clues only. However recent_paths are written verbatim into the pending entry Evidence block and evidence_paths are written into frontmatter, so credential-like or PII-bearing path strings can still land in a git-backed knowledge store despite the comment that secrets must never be persisted.","location":"packages/server/src/services/extract-knowledge.ts:277","fix_direction":"Include recent_paths, evidence_paths, relevance_paths, tags, tech_stack, and impact in the secret/privacy scan before rendering; redact or reject sensitive path metadata and add regression tests covering token-looking paths.","affected_components":["fab_extract_knowledge","knowledge pending entries","secret scan"]}]; severity_distribution={"critical":0,"high":0,"medium":3,"low":0}; error=}.findings

**Top Issues:**
- [medium] Session digests persist raw user transcript snippets without redaction or retention controls ($(@{title=Session digests persist raw user transcript snippets without redaction or retention controls; severity=medium; description=Stop-hook transcript summarization pushes raw user message text into user_messages, then session-digest-writer writes up to 10 messages of 500 chars each under .fabric/.cache/session-digests. The writer only truncates; it does not call the existing secret/PII redaction utilities and no TTL cleanup is configured for these digest files, so sensitive prompts can linger in local cache and later feed fabric-archive context.; location=packages/cli/templates/hooks/lib/session-digest-writer.cjs:71; fix_direction=Run digest content through the shared redactSecrets/privacy filter before write, add a configurable TTL/cleanup for .fabric/.cache/session-digests, and prefer summaries over verbatim user-message bullets.; affected_components=System.Object[]}.location))
- [medium] fab_review lifecycle events lack invoker session provenance ($(@{title=fab_review lifecycle events lack invoker session provenance; severity=medium; description=FabReviewInputSchema branches for approve/reject/defer contain only pending paths/reason/until and no session_id or actor field; the service then emits knowledge_proposed, knowledge_promote_started, knowledge_promoted, knowledge_rejected, and knowledge_deferred events with timestamp/reason but no session_id/correlation_id. The ledger records that a knowledge write happened but cannot attribute it to the invoking session or operator.; location=packages/shared/src/schemas/api-contracts.ts:907; fix_direction=Add optional session_id/correlation_id (and, if available, actor/client) to fab_review input, thread it through reviewKnowledge, and include it on all review mutation events; add tests asserting approve/reject/defer/promote events preserve provenance.; affected_components=System.Object[]}.location))
- [medium] Archive secret scan omits persisted path metadata fields ($(@{title=Archive secret scan omits persisted path metadata fields; severity=medium; description=extractKnowledge gates secret-shaped content by scanning user_messages_summary, session_context, must_read_if, and intent_clues only. However recent_paths are written verbatim into the pending entry Evidence block and evidence_paths are written into frontmatter, so credential-like or PII-bearing path strings can still land in a git-backed knowledge store despite the comment that secrets must never be persisted.; location=packages/server/src/services/extract-knowledge.ts:277; fix_direction=Include recent_paths, evidence_paths, relevance_paths, tags, tech_stack, and impact in the secret/privacy scan before rendering; redact or reject sensitive path metadata and add regression tests covering token-looking paths.; affected_components=System.Object[]}.location))

## Issues Created
- ISS-20260608-016: [medium] Store alias path traversal escapes by-alias symlink directory
- ISS-20260608-017: [high] Store-era recall recomputes and rereads the full read-set corpus multiple times per request
- ISS-20260608-018: [high] Corrupt store counters fail open to zero and can re-mint existing stable_ids
- ISS-20260608-019: [high] store migrate removes sources before counters are reconciled, leaving an interruption window for duplicate IDs
- ISS-20260608-020: [medium] Global/project store config mutations use unlocked read-modify-write and can lose concurrent updates
- ISS-20260608-021: [medium] Retired install implementation remains beside live install-v2 and is still tested
- ISS-20260608-022: [medium] Client hook scripts are checked in as four identical source copies
- ISS-20260608-023: [medium] install-v2 runs semantic-search guidance twice after successful pipeline install
- ISS-20260608-024: [medium] `fabric info scope` without scope argument falls through to runtime crash
- ISS-20260608-025: [medium] Ink TUI status states rely on color and Unicode glyphs instead of spoken text labels
- ISS-20260608-026: [medium] Install TUI spinner animates Braille frames continuously with no reduced-motion or screen-reader-safe mode
- ISS-20260608-027: [low] OutputRendererConfig.colors is declared but InkOutputRenderer never applies it
- ISS-20260608-028: [medium] Session digests persist raw user transcript snippets without redaction or retention controls
- ISS-20260608-029: [medium] fab_review lifecycle events lack invoker session provenance
- ISS-20260608-030: [medium] Archive secret scan omits persisted path metadata fields

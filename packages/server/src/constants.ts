/**
 * Shared constants used across the server package.
 */

/** MCP resource URI for the project's bootstrap README (L0 rules) file. */
export const AGENTS_MD_RESOURCE_URI = "fabric://bootstrap-readme";

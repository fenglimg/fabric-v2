# fabric-retrieve scenarios (optional detail)

See parent `SKILL.md` for the mandatory default workflow and failure paths.
This file exists so install can ship a thin `ref/` directory like sibling skills
when needed; keep content short to stay under skill token budget.

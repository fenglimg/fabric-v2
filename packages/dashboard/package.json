{
  "name": "@fenglimg/fabric-dashboard",
  "private": true,
  "version": "1.5.1",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@fenglimg/fabric-shared": "workspace:*",
    "@preact/signals-core": "^1.8.0",
    "preact": "^10.27.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.2",
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  }
}

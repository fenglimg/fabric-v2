{
  "name": "@fenglimg/fabric-server-http-experimental",
  "version": "0.0.0-quarantine",
  "private": true,
  "description": "Quarantined HTTP server + serve lock + bearer auth for Fabric v2.0.0. Reserved for future web UI restart. NOT built or tested by default; do not depend on this package from main.",
  "type": "module",
  "main": "./README.md",
  "scripts": {
    "build": "echo 'experimental package — skipped' && exit 0",
    "test": "echo 'experimental package — skipped' && exit 0"
  }
}

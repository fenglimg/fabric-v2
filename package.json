{
  "name": "fabric-monorepo",
  "private": true,
  "version": "1.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r --if-present test",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "lefthook": "^1.7.18",
    "vitest": "^3.2.4"
  }
}
